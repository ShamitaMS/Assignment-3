import "../styles/globals.css"; // Import global styles
import "../styles/Home.module.css"; // Import module styles if needed

function MyApp({ Component, pageProps }) {
  return <Component {...pageProps} />;
}

export default MyApp;
